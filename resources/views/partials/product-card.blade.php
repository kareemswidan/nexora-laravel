@php($ar=app()->getLocale()==='ar')<article class="product reveal"><a class="visual {{ $product->color }}" href="{{ route('product',$product) }}"><span class="badge">{{ $ar?$product->badge_ar:$product->badge }}</span><div class="product-icon">{{ $product->icon }}</div><span class="view-orbit">{{ __('ui.view') }} ↗</span></a><div class="product-info"><div class="category">{{ $ar?$product->category_ar:$product->category }}<b>★ {{ $product->rating }}</b></div><h3><a href="{{ route('product',$product) }}">{{ $ar?$product->name_ar:$product->name }}</a></h3><p>{{ $ar?$product->description_ar:$product->description }}</p><div class="buy"><strong>${{ number_format($product->price,0) }}<del>${{ number_format($product->old_price,0) }}</del></strong><a href="{{ route('product',$product) }}">{{ __('ui.view') }} ↗</a></div></div></article>

