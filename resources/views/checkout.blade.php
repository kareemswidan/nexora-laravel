@extends('layouts.app')@section('title',__('ui.secure'))@section('content')<div class="checkout-steps wrap"><span class="done">1 <b>{{ __('ui.cart') }}</b></span><i></i><span class="active">2 <b>Details</b></span><i></i><span>3 <b>Delivery</b></span></div><main class="checkout-page wrap"><form method="post" action="{{ route('checkout.store') }}" class="reveal">@csrf<span class="micro">SECURE CHECKOUT</span><h1>{{ __('ui.complete') }}</h1><p class="checkout-intro">Your digital toolkit is only a few seconds away.</p><label>{{ __('ui.name') }}<input name="customer_name" value="{{ old('customer_name') }}" required></label><label>{{ __('ui.email') }}<input name="email" type="email" value="{{ old('email') }}" required></label>@if($errors->any())<p class="form-error">{{ $errors->first() }}</p>@endif<button>{{ __('ui.place') }} · ${{ number_format($items->sum('line_total'),2) }} →</button><div class="checkout-seals"><span>🔒 SSL secured</span><span>↺ Support guarantee</span><span>⚡ {{ __('ui.instant') }}</span></div></form><aside class="reveal delay-1"><span class="micro">YOUR TOOLKIT</span><h2>{{ __('ui.summary') }}</h2>@foreach($items as $row)@php($p=$row['product'])<div class="summary-item"><i class="{{ $p->color }}">{{ $p->icon }}</i><span>{{ app()->isLocale('ar')?$p->name_ar:$p->name }} ×{{ $row['quantity'] }}</span><b>${{ number_format($row['line_total'],2) }}</b></div>@endforeach<div class="summary-total"><span>{{ __('ui.total') }}</span><b>${{ number_format($items->sum('line_total'),2) }}</b></div><p>✓ {{ __('ui.instant') }}<br>✓ {{ __('ui.verified') }}<br>✓ {{ __('ui.support') }}</p></aside></main>@endsection

