@extends('layouts.app')@section('title',__('ui.ai'))@section('content')<main class="ai-page wrap"><div class="reveal"><span class="micro">{{ __('ui.ai_label') }}</span><h1>{{ __('ui.stop') }}<br>{{ __('ui.start') }}</h1><p>{{ __('ui.describe') }}</p><ul><li>✓ {{ __('ui.no_key') }}</li><li>✓ {{ __('ui.two_lang') }}</li><li>✓ {{ __('ui.no_history') }}</li></ul><div class="ai-proof"><span><b>6</b> {{ __('ui.tools') }}</span><span><b>&lt;2s</b> {{ __('ui.speed') }}</span><span><b>100%</b> {{ __('ui.private') }}</span></div></div><form class="ai-console reveal delay-1" method="post" action="{{ route('ai.recommend') }}">@csrf<div class="ai-status"><span><i></i> {{ __('ui.online') }}</span><small>{{ __('ui.local') }}</small></div><div class="ai-mark">✦</div><h2>{{ __('ui.building') }}</h2><textarea name="query" required placeholder="{{ __('ui.placeholder') }}">{{ $query??'' }}</textarea><button class="ai-submit">{{ __('ui.build') }} ↗</button>@isset($products)<div class="answer"><b>✦ {{ __('ui.recommends') }}</b><p>@foreach($products as $p){{ app()->isLocale('ar')?$p->name_ar:$p->name }} (${{ number_format($p->price,0) }}){{ !$loop->last?' + ':'' }}@endforeach</p><a href="{{ route('shop') }}">{{ __('ui.marketplace') }} →</a></div>@endisset</form></main>@endsection
