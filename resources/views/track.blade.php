@extends('layouts.app')@section('title',__('ui.track'))@section('content')<main class="narrow-page order-track"><span class="micro">ORDER TRACKING</span><h1>{{ __('ui.where') }}</h1><p>Enter your order number and the email used at checkout.</p><form method="post" action="{{ route('orders.find') }}">@csrf<input name="order_number" required placeholder="NX-260713-ABC123"><input name="email" type="email" required placeholder="{{ __('ui.email') }}"><button>{{ __('ui.find') }} ↗</button></form>@if(isset($searched)&&!$order)<p class="form-error">No matching order found.</p>@endif @if(isset($order)&&$order)<div class="order-result"><i>✓</i><div><span>{{ $order->order_number }}</span><h2>{{ ucfirst($order->status) }}</h2><p>${{ number_format($order->total,2) }}</p></div></div>@endif</main>@endsection

